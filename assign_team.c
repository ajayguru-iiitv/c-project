/*this code is used create a new database for a new project 
we will deploy a sorting algorithm that will automatically assign a team sorting algorithm 
will work on the ratings which will be in the form of integers*/
#include <stdio.h>
int main(){
	printf("%s\n", );
}